class Point {
  // Declaring variables
  int x, y;

  // Empty constructor used for fruit
  Point() {
  }

  // Constructor used for snake length
  Point(int tempX, int tempY) {
    x = tempX;
    y = tempY;
  }
}